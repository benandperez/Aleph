<form method="post" action="{{ path('app_achieved_goals_delete', {'id': achieved_goal.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ achieved_goal.id) }}">
    <button class="btn">Delete</button>
</form>
