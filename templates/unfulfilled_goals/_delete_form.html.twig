<form method="post" action="{{ path('app_unfulfilled_goals_delete', {'id': unfulfilled_goal.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ unfulfilled_goal.id) }}">
    <button class="btn">Delete</button>
</form>
