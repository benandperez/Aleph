<form method="post" action="{{ path('app_labor_information_aleph_group_delete', {'id': labor_information_aleph_group.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ labor_information_aleph_group.id) }}">
    <button class="btn">Delete</button>
</form>
